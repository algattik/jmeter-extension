{
  "$schema": "https://raw.githubusercontent.com/Microsoft/azure-pipelines-task-lib/master/tasks.schema.json",
  "id": "7b05c43b-e07e-43e1-8cfd-fa60802a18a9",
  "name": "TaurusRunner",
  "friendlyName": "Taurus tool runner",
  "description": "Run Taurus",
  "author": "Alexandre Gattiker",
  "helpMarkDown": "[Learn more about this task](https://aka.ms/custom-jmeter-tasks)",
  "category": "Utility",
  "visibility": [
    "Build",
    "Release"
  ],
  "demands": [],
  "version": {
    "Major": 0,
    "Minor": 1,
    "Patch": 1584529567
  },
  "instanceNameFormat": "Run Taurus",
  "inputs": [
    {
      "name": "taurusArguments",
      "type": "string",
      "label": "Taurus Arguments",
      "defaultValue": "taurus_file.yml jmeter_file.jmx http://website ...",
      "required": true,
      "helpMarkDown": "Files or websites to test. Can be one or more Taurus YAML definition files, JMeter files or website URLs."
    },
    {
      "name": "jmeterHome",
      "type": "string",
      "label": "JMeter Home",
      "defaultValue": "$(JMeterHome)",
      "required": true,
      "helpMarkDown": "The directory in which JMeter is installed. Use \"$(JMeterHome)\" to reference the output of the JMeter tool installer task."
    },
    {
      "name": "jmeterPath",
      "type": "string",
      "label": "JMeter Path",
      "defaultValue": "$(JMeterPath)",
      "required": true,
      "helpMarkDown": "The path in which JMeter is installed. Use \"$(JMeterPath)\" to reference the output of the JMeter tool installer task."
    },
    {
      "name": "jmeterVersion",
      "type": "string",
      "label": "JMeter Version",
      "defaultValue": "$(JMeterVersion)",
      "required": true,
      "helpMarkDown": "The installed JMeter version. Use \"$(JMeterVersion)\" to reference the output of the JMeter tool installer task."
    },
    {
      "name": "outputDir",
      "type": "string",
      "label": "Artifacts output directory",
      "defaultValue": "$(Build.StagingDirectory)/taurus",
      "required": true,
      "helpMarkDown": "The Taurus and JMeter output directory."
    }
  ],
  "execution": {
    "Node10": {
      "target": "src/index.js"
    }
  }
}
